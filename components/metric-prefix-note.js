import React from 'react'
function MetricPrefixNote(props) {
  return <p style={{ fontSize: '0.9em' }}>Metric prefixes such as u (micro), m (milli), k (kilo), and M (mega) are allowed after the number.</p>
}

export default MetricPrefixNote